import { GestionRessource, type ChampGestion } from "@/components/gestion-ressource"
import { autorisationsFrontend } from "@/lib/frontend-api/autorisation"
import { apiFrontend } from "@/lib/frontend-api/ressources"

export default async function Page(){const[o,m,n,d]=await Promise.all([apiFrontend.ouvrages({limite:100}),apiFrontend.matieres({limite:100}),apiFrontend.niveaux({limite:100}),autorisationsFrontend()]);const champs:ChampGestion[]=[{nom:"titre",libelle:"Titre",requis:true},{nom:"sous_titre",libelle:"Sous-titre"},{nom:"isbn",libelle:"ISBN"},{nom:"editeur",libelle:"Éditeur"},{nom:"edition",libelle:"Édition"},{nom:"annee_publication",libelle:"Année de publication",type:"number"},{nom:"matiere_id",libelle:"Matière",type:"select",options:m.donnees.map(x=>({valeur:x.id,libelle:x.nom}))},{nom:"niveau_scolaire_id",libelle:"Niveau scolaire",type:"select",requis:false,options:[{valeur:"",libelle:"Sans niveau — tous les élèves"},...n.donnees.map(x=>({valeur:x.id,libelle:x.nom}))]},{nom:"description",libelle:"Description",type:"textarea"},{nom:"est_actif",libelle:"Actif",type:"checkbox"}];const lignes=o.donnees.map(x=>({...x,groupe_niveau:n.donnees.find(y=>y.id===x.niveau_scolaire_id)?.nom??"Sans niveau",groupe_matiere:m.donnees.find(y=>y.id===x.matiere_id)?.nom??"Sans matière",resume_affichage:`${x.nombre_exemplaires??0} exemplaire(s) · ${x.nombre_disponibles??0} disponible(s) · ${x.nombre_empruntes??0} emprunté(s)`}));return <GestionRessource peutModifier={d.peutModifier} ressource="ouvrages" titre="Ouvrages" lignes={lignes} champs={champs}/>}
