export function assertSignupAllowed(input:{email:string;publicSignupEnabled:boolean;activationEmail?:string;bootstrapEmail?:string}){const email=input.email.toLowerCase(),interne=input.activationEmail?.toLowerCase()===email||input.bootstrapEmail?.toLowerCase()===email;if(!interne&&!input.publicSignupEnabled)throw new Error("L’inscription publique est désactivée");return{internal:interne}}
